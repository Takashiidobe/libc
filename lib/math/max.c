#include "../../include/math/max.h"

int maxi(int const x, int const y) { return x > y ? x : y; }

unsigned maxu(unsigned const x, unsigned const y) { return x > y ? x : y; }

long maxl(long const x, long const y) { return x > y ? x : y; }

unsigned long maxul(unsigned long const x, unsigned long const y) {
  return x > y ? x : y;
}

long long maxll(long long const x, long long const y) { return x > y ? x : y; }

unsigned long long maxull(unsigned long long const x,
                          unsigned long long const y) {
  return x > y ? x : y;
}

float maxf(float const x, float const y) { return x > y ? x : y; }

double maxd(double const x, double const y) { return x > y ? x : y; }

long double maxld(long double const x, long double const y) {
  return x > y ? x : y;
}
